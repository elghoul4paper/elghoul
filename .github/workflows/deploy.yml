name: Deploy

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest  # أو windows-latest حسب نظام التشغيل

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # 🔹 تأكد من استخدام أحدث إصدار

      - name: Setup Node.js
        uses: actions/setup-node@v4  # 🔹 تأكد من أن الإصدار صحيح
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      - name: Build Project
        run: npm run build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v3  # 🔹 تأكد من صحة الإصدار
        with:
          name: build-files
          path: dist/  # 🔹 تحقق من أن هذا المسار صحيح

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v3  # 🔹 تأكد من صحة الإصدار
        with:
          name: build-files
          path: dist/

      - name: Deploy Application
        run: echo "Deploying application..."
